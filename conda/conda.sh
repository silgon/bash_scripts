#!/usr/bin/env bash

# path where ros distros are installed (default one is usually correct)
export CONDA_INSTALL_PATH="$HOME/anaconda"

# path where ros development is done
export CONDA_BASE_PATH="$HOME/anaconda"
